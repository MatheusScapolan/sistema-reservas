<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recuperar Senha - Sistema de Reservas INTELI</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <!-- INTELI Theme CSS -->
    <link rel="stylesheet" href="/css/inteli-theme.css">

    <!-- CSS customizado para forgot-password -->
    <style>
        body {
            background: var(--inteli-roxo);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: var(--font-primary);
        }

        .forgot-password-container {
            width: 100%;
            max-width: 400px;
            padding: 20px;
        }

        .forgot-password-card {
            background: var(--inteli-branco);
            border-radius: 16px;
            padding: 40px 30px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .logo-container {
            margin-bottom: 30px;
            display: flex;
            justify-content: center;
        }

        .inteli-logo {
            max-width: 150px;
            height: auto;
            width: auto;
            filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.1));
        }

        .welcome-title {
            font-family: var(--font-secondary);
            font-weight: 700;
            font-size: 28px;
            color: var(--inteli-roxo);
            margin-bottom: 10px;
        }

        .welcome-subtitle {
            font-family: var(--font-primary);
            font-size: 14px;
            color: var(--inteli-cinza-escuro);
            margin-bottom: 30px;
            line-height: 1.5;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        .form-label {
            font-family: var(--font-primary);
            font-weight: 600;
            font-size: 14px;
            color: var(--inteli-roxo);
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .form-control {
            border: 2px solid var(--inteli-cinza);
            border-radius: 8px;
            padding: 12px 16px;
            font-family: var(--font-primary);
            font-size: 14px;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: var(--inteli-azul);
            box-shadow: 0 0 0 0.2rem rgba(20, 38, 171, 0.25);
        }

        .form-text {
            font-family: var(--font-primary);
            font-size: 12px;
            color: var(--inteli-cinza-escuro);
            margin-top: 5px;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .btn-forgot {
            background: var(--inteli-coral);
            border: none;
            border-radius: 8px;
            padding: 12px 24px;
            font-family: var(--font-primary);
            font-weight: 600;
            font-size: 16px;
            color: white;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .btn-forgot:hover {
            background: var(--inteli-azul);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(255, 69, 69, 0.3);
            color: var(--inteli-branco);
        }

        .btn-forgot:disabled {
            background: var(--inteli-cinza);
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        .login-link {
            font-family: var(--font-primary);
            font-size: 14px;
            color: var(--inteli-cinza-escuro);
            margin-top: 20px;
        }

        .login-link a {
            color: var(--inteli-azul);
            text-decoration: none;
            font-weight: 600;
        }

        .login-link a:hover {
            color: var(--inteli-lilas);
            text-decoration: underline;
        }

        .security-info {
            margin-top: 25px;
            padding-top: 20px;
            border-top: 1px solid var(--inteli-cinza);
            text-align: center;
        }

        .security-item {
            font-family: var(--font-primary);
            font-size: 12px;
            color: var(--inteli-cinza-escuro);
            margin-bottom: 8px;
            display: inline-flex;
            align-items: center;
            gap: 5px;
            margin: 0 15px 8px 15px;
        }

        .alert {
            border-radius: 8px;
            padding: 12px 16px;
            margin-bottom: 20px;
            font-family: var(--font-primary);
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .alert-danger {
            background-color: rgba(220, 53, 69, 0.1);
            color: var(--inteli-vermelho);
            border: 1px solid rgba(220, 53, 69, 0.2);
        }

        .alert-success {
            background-color: rgba(40, 167, 69, 0.1);
            color: #155724;
            border: 1px solid rgba(40, 167, 69, 0.2);
            word-wrap: break-word;
            word-break: break-all;
            max-width: 100%;
            overflow-wrap: break-word;
        }

        .alert-success a {
            color: #0c5460;
            text-decoration: underline;
            font-weight: 600;
        }

        .alert-success a:hover {
            color: #155724;
            text-decoration: none;
        }

        /* Responsividade */
        @media (max-width: 480px) {
            .forgot-password-container {
                padding: 15px;
            }

            .forgot-password-card {
                padding: 30px 20px;
            }

            .inteli-logo {
                max-width: 120px;
            }

            .welcome-title {
                font-size: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="forgot-password-container">
        <div class="forgot-password-card">
            <!-- Logo INTELI -->
            <div class="logo-container">
                <img src="/images/inteli-logo.png" alt="INTELI Logo" class="inteli-logo">
            </div>

            <!-- Título -->
            <h1 class="welcome-title">Recuperar Senha</h1>
            <p class="welcome-subtitle">Digite seu email institucional para receber as instruções de recuperação de senha.</p>

            <!-- Mensagens de erro/sucesso -->
            <% if (error && error.length > 0) { %>
                <div class="alert alert-danger" role="alert">
                    <i class="fas fa-exclamation-triangle"></i>
                    <%= error %>
                </div>
            <% } %>
            <% if (message && message.length > 0) { %>
                <div class="alert alert-success" role="alert">
                    <i class="fas fa-check-circle"></i>
                    <%= message %>
                </div>
            <% } %>

            <!-- Formulário -->
            <form action="/forgot-password" method="POST" id="forgotPasswordForm">
                <div class="form-group">
                    <label for="email" class="form-label">
                        <i class="fas fa-envelope"></i>
                        Email Institucional
                    </label>
                    <input type="email"
                           class="form-control"
                           id="email"
                           name="email"
                           pattern=".*@sou\.inteli\.edu\.br$"
                           title="Apenas emails do domínio @sou.inteli.edu.br são permitidos"
                           placeholder="seu.nome@sou.inteli.edu.br"
                           required>
                    <div class="form-text">
                        <i class="fas fa-info-circle"></i>
                        Apenas emails do domínio @sou.inteli.edu.br são permitidos
                    </div>
                </div>

                <button type="submit" class="btn-forgot" id="submitBtn">
                    <span class="spinner-border spinner-border-sm d-none" role="status" aria-hidden="true"></span>
                    <i class="fas fa-paper-plane" id="submitIcon"></i>
                    <span id="submitText">Enviar Instruções</span>
                </button>
            </form>

            <!-- Link para login -->
            <div class="login-link">
                Lembrou da senha? <a href="/login">Fazer Login</a>
            </div>

            <!-- Informações de segurança -->
            <div class="security-info">
                <div class="security-item">
                    <i class="fas fa-shield-alt"></i>
                    Link seguro
                </div>
                <div class="security-item">
                    <i class="fas fa-clock"></i>
                    Expira em 1 hora
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.getElementById('forgotPasswordForm');
            const submitBtn = document.getElementById('submitBtn');
            const spinner = submitBtn.querySelector('.spinner-border');

            form.addEventListener('submit', function() {
                submitBtn.disabled = true;
                spinner.classList.remove('d-none');
                submitBtn.innerHTML = '<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Enviando...';
            });
        });
    </script>
</body>
</html>
